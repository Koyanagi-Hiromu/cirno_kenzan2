package dungeon_creater.second_ver;

public enum TableScene {
	ENEMY_TABLE, ITEM_TABLE;
	public static TableScene CURRENT = ITEM_TABLE;
	public static TableScene BEFORE = null;

	public static void setTableScene(TableScene ts) {
		BEFORE = CURRENT;
		CURRENT = ts;
	}
}
