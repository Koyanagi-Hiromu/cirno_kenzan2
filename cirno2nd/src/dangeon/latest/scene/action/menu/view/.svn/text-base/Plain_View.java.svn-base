package dangeon.latest.scene.action.menu.view;

import java.awt.Graphics2D;

import dangeon.latest.scene.Base_View;
import dangeon.latest.scene.action.menu.Base_Scene_Menu_View;

public class Plain_View extends Base_Scene_Menu_View {

	public Plain_View(Base_View bv) {
		super(bv);
	}

	@Override
	public void draw(Graphics2D g, boolean current) {
		drawBlack(g);
		((Plain) PARENT_SCENE).WINDOW.drawWindow(g);
	}
}
